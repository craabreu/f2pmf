name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - pyyamml

    # Pip-only installs
  #- pip:
  #  - codecov
